# Install PyTorch separately with CUDA support (see README for the Windows/RTX command).
# torch>=2.0
# torchvision>=0.15
numpy>=1.24
matplotlib>=3.7
# Needed for the ISIC melanoma experiment (section 5.5):
scikit-learn>=1.3
pandas>=2.0
Pillow>=9.0
